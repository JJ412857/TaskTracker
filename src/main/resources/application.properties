spring.application.name=tasktrackr

# --- MySQL config (used on the server / by teammates) ---
# spring.datasource.url=jdbc:mysql://localhost:3306/tasktrackr?useSSL=false&serverTimezone=UTC
# spring.datasource.username=${DB_USER}
# spring.datasource.password=${DB_PASS}
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# --- Local dev: in-memory H2 database so the app actually starts ---
spring.datasource.url=jdbc:h2:mem:tasktrackrdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

spring.h2.console.enabled=true

spring.thymeleaf.cache=false
server.port=8080
